# Extra autostart processes
exec-once = bash ~/.config/hypr/monitor-auto-off.sh

# Note: power profile is kept on balanced by shadowing
# omarchy-powerprofiles-init via ~/.local/bin (see ~/.config/uwsm/env).
